import Header from '../components/Header';

const HomePage = () => {
  return (
    <div className="min-h-screen flex flex-col bg-light-sky">
      <Header variant="sm">something</Header>
    </div>
  );
};

export default HomePage;
